# XKCD App ToDo — 2026-02-27

## Done
- Previous / Next buttons (commit `a3c7673`)
- Copy link (commit `56927c1`)
- Search by date (commit `56927c1`)

## Still Remaining

### Navigation
- Jump to a specific comic number directly (date search exists, but not number input)

### Discovery
- "Explain this comic" — button/link to explainxkcd.com
- Favorites / bookmarks

### Display
- Pinch-to-zoom on the image (no `InteractiveViewer` wrapping the image)
- Show the comic **title** — currently the AppBar shows `xkAlt` (the hover/alt text); the `title` field from the API is never stored or displayed
- Dark mode support (theme is hardcoded)

### Sharing
- Native share sheet (only clipboard copy is implemented)

### UX polish
- Loading spinner while the *image* loads (metadata spinner exists via `isSearching`, but `Image.network` has no loading placeholder)
- Error handling for failed network requests
- Offline support / caching
